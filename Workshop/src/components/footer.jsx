import "../styles/footer.css"

const Footer = () => {
    return(
        <footer>
            {/*Add the header for footer and label it properly*/}
            <div className="socialSection">
                <div className="socials">
                    {/* Add logos thats embedded with a link and a label
                    */}  
                </div>
                {/*Using the box model how you would structure your HTML to add the newsletter header and button to the parent div?*/}
            </div>
        </footer>
    )
}

export default Footer;